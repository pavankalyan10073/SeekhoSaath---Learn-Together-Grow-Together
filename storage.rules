rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /tutors/{tutorId}/{file} {
      allow read: if request.auth != null && request.auth.uid == tutorId;
      allow write: if request.auth != null && request.auth.uid == tutorId;
    }
  }
}
